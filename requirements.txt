# DiffRS — reflection removal inference dependencies
# Tested with Python 3.10 + CUDA 12.1 on NVIDIA L20.
#
# Install PyTorch first (CUDA 12.1 build), then the rest:
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# Note: the Stable Diffusion 2 base model is downloaded automatically on first
# run from a public repo (default sd2-community/stable-diffusion-2-1); no login
# is required. Override with --base_model if you prefer your own SD-2 weights.

# --- Core (install via the cu121 index URL above; pinned here for reference) ---
torch==2.5.1
torchvision==0.20.1

# --- Diffusion stack (versions must stay mutually compatible) ---
diffusers==0.31.0
transformers==4.46.3
tokenizers==0.20.3
huggingface-hub==0.26.2
safetensors==0.7.0

# --- Latent optimization (--optimization uses bnb.optim.AdamW8bit) ---
bitsandbytes==0.49.2

# --- Recommended: faster / lower-memory model loading ---
accelerate==1.13.0

# --- Utilities ---
numpy==2.2.6
Pillow==12.2.0
tqdm==4.67.3
